export interface GlobalStats {
    _id: string;
    totalMarketCap: number;
    // include other properties that globalStats might have
  }
  
  export interface StablecoinStats {
    _id: string;
    totalMarketCapUSD: number;
    // include other properties that stablecoinStats might have
  }